<h4>SEE ALL OF YOUR CONTACTS' TAGS BELOW:</h4>

<div id="tag_cloud">
  <% tag_cloud Contact.tag_counts, %w[s m l] do |tag, css_class| %>
    <%= link_to tag.name, tag_path(tag.name), class: css_class %>
  <% end %>
</div>

<br/>